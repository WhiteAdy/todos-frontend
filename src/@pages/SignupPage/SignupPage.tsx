import { Card } from '@components';
import styles from './SignupPage.module.scss';

function SignupPage() {
	return (
		<div className={styles.SignupPage}>
			<Card>Signup Page</Card>
		</div>
	);
}

export default SignupPage;
